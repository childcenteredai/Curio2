You are given a knowledge graph (KG) of scientific components. Your task is to determine whether the child’s latest question in the conversation directly mentions or implies a component that exists in the KG. 

<Retrieval Rules>
1. Check question validity
- If the child’s latest message is NOT a question, return an empty string (e.g., '') immediately and stop.

2. Match against the KG
- Focus only on the child's most recent question.
- Identify whether the question explicitly mentions or implicitly implies any component in the KG.
- If a strongly related component is found, retrieve and return the original content of that knowledge component from the KG.
- If no strong match exists (e.g., the relation of the child's question and knowledge component involves multiple logical connections), or the child's question is arbitrary, return an empty string (e.g., '').

3. Matching criteria
- Consider both semantic similarity (meaning-based) and lexical overlap (word-based) when identifying a match.
- Do not infer beyond what the question suggests; only match when the child's wording clearly connects to a component in the KG.
- **IMPORTANT: The knowledge components are organized by level of complexity and age-appropriateness, with simpler, foundational components listed FIRST and more advanced components listed LATER in the list.**
- **When selecting a component to match:**
  - **Count the number of turns in the conversation (each user message + assistant response = 1 turn).**
  - **If the conversation is within the first 5 turns, prioritize matching components that appear EARLIER in the list (simpler, foundational concepts).**
  - **If the conversation has more than 5 turns, you may consider components from the entire list, but still prioritize simpler components unless the child's question clearly requires a more advanced concept.**
- **Select the SINGLE most relevant component that best matches the child's question. Only return one component, not multiple.**

4. Relevance prioritization
- Prioritize components that directly answer what the child is asking about.
- Consider the child's age (8-10 years old) and choose age-appropriate concepts.
- If multiple components seem relevant, choose the one that:
  a) Most directly addresses the child's question
  b) Is simpler and more foundational (appears earlier in the list)
  c) Best matches the semantic meaning of the child's question
</Retrieval Rules>

<Conversation History>

</Conversation History>

<Child's Question>

</Child's Question>

<Response Format>
- **Return ONLY ONE component name as a single-item list (e.g., ["static electricity"]).**
- **Do NOT return multiple components. Select the single most relevant component.**
- The component name must match exactly as it appears in the Knowledge Components list.
- If no component is strongly relevant, return an empty string (e.g., '')
</Response Format>
